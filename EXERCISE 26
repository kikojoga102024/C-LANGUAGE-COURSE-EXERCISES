#include <stdio.h>
int main() {
	int i, numero, soma;
	soma = 0;
	printf("Digite 10 numeros e ao final calcule a soma dos numeros digitados.\n");
	for (i = 0; i < 10; i++) {
		scanf_s("%d", &numero);
		soma = soma + numero;
	}
	printf("A soma dos numeros digitados = %d\n", soma);
	return 0;
}
